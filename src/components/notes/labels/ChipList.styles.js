import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    position: 'relative',
    zIndex: 1100,
  },
  chip: {
    margin: '5px 0 5px 5px',
  },
}));
